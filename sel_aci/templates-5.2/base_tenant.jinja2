{
    "totalCount": "1",
    "imdata": [
        {
            "fvTenant": {
                "attributes": {
                },
                "children": [
                    {% for ITEM in ITEMS %}
                    {{ ITEM|indent(20) }}{{ "," if not loop.last }}
                    {% endfor %}
                ]
            }
        }
    ]
}