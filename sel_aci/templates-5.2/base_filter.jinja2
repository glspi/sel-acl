{
    "vzFilter": {
        "attributes": {
            "annotation": "",
            "descr": "",
            "name": "{{ FILTER_NAME }}"
        },
        "children": [
            {% for ITEM in ITEMS %}
            {{ ITEM|indent(12) }}{{ "," if not loop.last }}
            {% endfor %}
        ]
    }
}